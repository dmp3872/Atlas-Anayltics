/**
 * Lab feature toggles — flip these to hide experiments without deleting code.
 *
 * Print pack: set LAB_PRINT_PACK_ENABLED to false to revert the receive-desk
 * print stickers / vial labels / receipt sheet UI.
 */
export const LAB_PRINT_PACK_ENABLED = true;
